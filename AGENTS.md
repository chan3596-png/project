# AGENTS.md

## 목적

교육용 반도체 공정 AI MVP를 재현 가능하고 안전하게 개발한다.

## 필수 규칙

1. 추측하지 말고 불명확한 요구와 가정을 먼저 기록한다.
2. `data/raw/` 원본은 수정하지 않는다. 제외·보정은 코드와 로그로 남긴다.
3. 분석 전 스키마·결측·중복·단위·이상치·Lot/설비 편중을 점검한다.
4. 학습 데이터와 평가 데이터를 분리하고 기준모델과 비교한다.
5. 상관관계를 인과관계로 표현하지 않고 대안 가설을 최소 1개 검토한다.
6. AI가 생성한 코드·수치·해석은 테스트와 원자료로 검증한다.
7. 요청된 범위만 최소 변경하고 수정 후 테스트한다.
8. 실제 회사 데이터·내부 Spec·개인정보·API Key를 저장하거나 전송하지 않는다.
9. [보안] 웹/HTML 코드 작성 시 악성코드가 유포되는 `polyfill.io` CDN은 절대 사용하지 않는다.
10. 데이터 리포팅을 위한 `index.html` 파일 생성 시, 항상 기준이 되는 데이터(`*.csv`) 파일과 동일한 폴더에 저장한다.

## 완료 기준

- 로컬 빌드와 테스트 통과
- 입력값 변경 시 결과·위험도·추천이 갱신
- 기준모델과 평가 성능 표시
- AI 오류와 인간 수정 내용을 `AI_USAGE.md`에 기록
- README 실행법과 한계가 실제 동작과 일치
